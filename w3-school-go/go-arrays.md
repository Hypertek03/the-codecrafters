## Arrays
There are used to store multiple values of the same type in a single variable, instead of declaring separate variables for each value.

There are two ways in which one can declare an arrays:

1. The var keyword: example, var array_name = [length]datatype{values}

2. The := sign: example, array_name := [length]datatype{values}